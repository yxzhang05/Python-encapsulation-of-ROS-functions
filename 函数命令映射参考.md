# 函数命令映射参考文档

## 文档说明

本文档详细列出每个Python封装函数等价于哪些终端命令，以及如何处理返回的数据。

**用途**：
- 在终端中测试命令是否正确
- 排查问题时对比终端和Python结果  
- 理解函数的底层实现
- 学习ROS2命令使用

**使用方法**：
1. 查找函数名称
2. 复制"等价终端命令"
3. 在终端中执行测试
4. 对比"返回示例"
5. 确认命令正确后运行Python代码

---

## 系统管理函数

### 1. initialize(robot_type)

**功能**: 初始化机器人系统，启动底盘驱动

**等价终端命令**:
```bash
# 麦轮车型(mec):
ros2 launch turn_on_wheeltec_robot turn_on_wheeltec_robot.launch.py robot_name:=wheelTec_robot

# 阿克曼车型(akm):
ros2 launch turn_on_wheeltec_robot turn_on_wheeltec_robot.launch.py robot_name:=akm_robot

# 差速车型(diff):
ros2 launch turn_on_wheeltec_robot turn_on_wheeltec_robot.launch.py robot_name:=diff_robot
```

**终端测试**:
```bash
# 手动启动（以麦轮为例）
ros2 launch turn_on_wheeltec_robot turn_on_wheeltec_robot.launch.py robot_name:=wheelTec_robot

# 验证：检查节点
ros2 node list
ros2 topic list
```

---

### 2. get_battery_voltage()

**功能**: 获取电池电压

**等价终端命令**:
```bash
ros2 topic echo /PowerVoltage --once
```

**数据处理**: 读取话题，提取data字段的浮点数值

**终端测试**:
```bash
ros2 topic echo /PowerVoltage --once
# 输出示例：
# data: 12.5
```

---

### 3. emergency_stop()

**功能**: 紧急停止

**等价终端命令**:
```bash
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.0}, angular: {z: 0.0}}" --once
```

---

### 4. get_software_version()

**功能**: 获取软件版本

**等价终端命令**:
```bash
ros2 --version
ros2 pkg list | grep wheeltec
```

---

### 5. start_keyboard_control()

**功能**: 启动键盘遥控

**等价终端命令**:
```bash
ros2 run teleop_twist_keyboard teleop_twist_keyboard
```

---

## 底盘运动函数

### 1. set_velocity(v_x, v_y, w_z)

**功能**: 设置机器人速度

**等价终端命令**:
```bash
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist   "{linear: {x: 0.5, y: 0.0, z: 0.0}, angular: {x: 0.0, y: 0.0, z: 0.5}}" --once
```

**参数**:
- v_x: 前后速度(m/s)
- v_y: 左右速度(m/s，仅麦轮)
- w_z: 旋转角速度(rad/s)

**终端测试**:
```bash
# 前进
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.3}}" --once

# 左移（麦轮）
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {y: 0.2}}" --once

# 旋转
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{angular: {z: 0.5}}" --once

# 停止
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{}" --once
```

---

### 2. move_distance(distance, speed)

**功能**: 移动指定距离

**等价终端命令**（组合逻辑）:
```bash
# 1. 获取当前位置
ros2 topic echo /odom --once

# 2. 发送速度命令
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.3}}" --rate 10 &

# 3. 监控位置
ros2 topic echo /odom --rate 2 | grep "position:"

# 4. 达到距离后停止
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{}" --once
```

**数据处理**:
1. 读取起始位置
2. 循环发送速度命令
3. 计算已移动距离
4. 到达目标停止

---

### 3. rotate_angle(angle_degrees, angular_speed)

**功能**: 原地旋转指定角度

**等价终端命令**（组合逻辑）:
```bash
# 1. 获取当前朝向
ros2 topic echo /odom --once | grep "orientation:"

# 2. 发送旋转命令
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{angular: {z: 0.5}}" --rate 10 &

# 3. 监控朝向
ros2 topic echo /odom --rate 2

# 4. 达到角度后停止
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{}" --once
```

**数据处理**:
1. 读取起始朝向（四元数）
2. 转换为欧拉角
3. 循环发送角速度
4. 计算已旋转角度
5. 到达目标停止

---

### 4. get_wheel_speeds()

**功能**: 获取车轮速度

**等价终端命令**:
```bash
ros2 topic echo /wheel_speeds --once
# 或
ros2 topic echo /joint_states --once
```

---

### 5. get_imu_data()

**功能**: 获取IMU数据

**等价终端命令**:
```bash
ros2 topic echo /imu/data_raw --once
```

**返回数据**:
- angular_velocity: 角速度(rad/s)
- linear_acceleration: 加速度(m/s²)

**终端测试**:
```bash
ros2 topic echo /imu/data_raw --once
# 输出包含：
# angular_velocity: {x, y, z}
# linear_acceleration: {x, y, z}
```

---

### 6. get_robot_pose()

**功能**: 获取机器人位姿

**等价终端命令**:
```bash
ros2 topic echo /odom --once
```

**数据处理**:
1. 读取/odom话题
2. 提取position (x, y)
3. 提取orientation四元数
4. 转换为欧拉角(yaw)

**终端测试**:
```bash
ros2 topic echo /odom --once
# 查看：
# pose.pose.position: {x, y, z}
# pose.pose.orientation: {x, y, z, w}
```

**四元数转yaw公式**:
```
yaw = atan2(2*(w*z + x*y), 1 - 2*(y² + z²))
```

---

## 机械臂控制函数

### 1. arm_home()

**功能**: 机械臂归零

**等价终端命令**:
```bash
ros2 topic pub /joint_command sensor_msgs/msg/JointState   "{name: ['joint1', 'joint2', 'joint3', 'joint4'], position: [0.0, 0.0, 0.0, 0.0]}" --once
```

---

### 2. set_joint_angles(angles)

**功能**: 设置关节角度

**等价终端命令**:
```bash
ros2 topic pub /joint_command sensor_msgs/msg/JointState   "{name: ['joint1', 'joint2', 'joint3', 'joint4'], position: [0.785, 1.571, -0.785, 0.0]}" --once
```

**数据处理**: 角度(度) → 弧度 (角度 × π/180)

---

### 3. set_gripper(position)

**功能**: 控制夹爪

**等价终端命令**:
```bash
ros2 topic pub /gripper_command std_msgs/msg/Float64 "{data: 0.5}" --once
```

**参数**: 0.0(闭合) ~ 1.0(打开)

**终端测试**:
```bash
# 打开
ros2 topic pub /gripper_command std_msgs/msg/Float64 "{data: 1.0}" --once

# 闭合
ros2 topic pub /gripper_command std_msgs/msg/Float64 "{data: 0.0}" --once
```

---

### 4. get_joint_states()

**功能**: 获取关节状态

**等价终端命令**:
```bash
ros2 topic echo /joint_states --once
```

**返回数据**:
- name: 关节名称列表
- position: 位置(弧度)
- velocity: 速度
- effort: 力矩

---

## 感知功能函数

### 1. launch_lidar()

**功能**: 启动雷达

**等价终端命令**:
```bash
ros2 launch turn_on_wheeltec_robot lidar.launch.py
```

**终端测试**:
```bash
ros2 launch turn_on_wheeltec_robot lidar.launch.py

# 验证
ros2 topic list | grep scan
ros2 topic echo /scan
```

---

### 2. launch_camera()

**功能**: 启动相机

**等价终端命令**:
```bash
ros2 launch turn_on_wheeltec_robot camera.launch.py
```

**终端测试**:
```bash
ros2 launch turn_on_wheeltec_robot camera.launch.py

# 验证
ros2 topic list | grep camera
ros2 run rqt_image_view rqt_image_view
```

---

### 3. get_lidar_distance()

**功能**: 获取雷达距离数据

**等价终端命令**:
```bash
ros2 topic echo /scan --once
```

**数据处理**:
1. 读取/scan话题（LaserScan消息）
2. 提取ranges数组
3. 计算前后左右方向距离
4. 找出最小距离

**终端测试**:
```bash
ros2 topic echo /scan --once
# 查看：
# ranges: [2.5, 2.6, ..., inf]
```

**方向定义**:
- front: 0° (机器人前方)
- back: 180°
- left: 90°
- right: -90°

---

### 4. start_visual_follow()

**功能**: 启动视觉跟随

**等价终端命令**:
```bash
ros2 launch wheeltec_visual visual_follow.launch.py
```

---

### 5. start_line_tracking()

**功能**: 启动视觉巡线

**等价终端命令**:
```bash
ros2 launch wheeltec_visual line_tracking.launch.py
```

---

### 6. start_lidar_follow()

**功能**: 启动雷达跟随

**等价终端命令**:
```bash
ros2 launch wheeltec_lidar lidar_follow.launch.py
```

---

## 建图导航函数

### 1. start_mapping()

**功能**: 启动SLAM建图

**等价终端命令**:
```bash
ros2 launch wheeltec_slam_toolbox slam_toolbox.launch.py
```

**终端测试**:
```bash
ros2 launch wheeltec_slam_toolbox slam_toolbox.launch.py

# 验证
ros2 topic echo /map
rviz2  # 查看地图
```

---

### 2. save_map(map_name)

**功能**: 保存地图

**等价终端命令**:
```bash
ros2 run nav2_map_server map_saver_cli -f /path/to/map_name
```

**终端测试**:
```bash
ros2 run nav2_map_server map_saver_cli -f ~/my_map

# 生成文件：
# ~/my_map.pgm  - 地图图像
# ~/my_map.yaml - 地图配置
```

---

### 3. load_map_and_start_navigation(map_file)

**功能**: 加载地图并启动导航

**等价终端命令**:
```bash
ros2 launch nav2_bringup navigation_launch.py map:=/path/to/map.yaml
```

---

### 4. move_to_goal(x, y, yaw)

**功能**: 导航到目标点

**等价终端命令**:
```bash
ros2 action send_goal /navigate_to_pose nav2_msgs/action/NavigateToPose   "{pose: {header: {frame_id: 'map'},     pose: {position: {x: 2.0, y: 1.0}, orientation: {z: 0.707, w: 0.707}}}}"
```

**数据处理**:
1. 欧拉角yaw → 四元数(z, w)
2. 构造NavigateToPose目标
3. 发送action请求
4. 等待完成

**四元数计算**:
```
z = sin(yaw / 2)
w = cos(yaw / 2)
```

---

### 5. cancel_navigation()

**功能**: 取消导航

**等价终端命令**:
```bash
ros2 action send_goal --cancel /navigate_to_pose
```

---

## 快速命令参考

### 话题操作
```bash
ros2 topic list                    # 列出所有话题
ros2 topic echo /odom              # 查看话题数据
ros2 topic echo /odom --once       # 查看一次
ros2 topic type /cmd_vel           # 查看话题类型
ros2 topic hz /scan                # 查看发布频率
ros2 topic pub /cmd_vel ...        # 发布话题
```

### 节点操作
```bash
ros2 node list                     # 列出节点
ros2 node info /wheeltec_robot     # 查看节点信息
```

### Launch操作
```bash
ros2 launch <package> <file>       # 启动launch文件
```

### 调试工具
```bash
ros2 run tf2_tools view_frames     # 查看TF树
rqt                                # RQT工具
rviz2                              # 可视化工具
```

---

## 使用建议

### 测试流程

1. **查找命令**: 在本文档中找到函数对应的终端命令
2. **终端测试**: 复制命令到终端执行
3. **观察输出**: 查看命令输出是否正常
4. **对比示例**: 与文档中的示例对比
5. **运行Python**: 确认命令正确后运行Python代码
6. **验证一致**: 对比Python结果和终端输出

### 故障排查

如果Python函数返回异常：

1. 用对应的终端命令测试
2. 检查ROS环境是否正确
3. 检查底盘驱动是否启动
4. 检查话题是否存在

### 示例：排查get_battery_voltage()问题

```bash
# 1. Python返回None
voltage = robot.get_battery_voltage()  # 返回None

# 2. 终端测试
ros2 topic echo /PowerVoltage --once
# 错误: topic not found

# 3. 检查话题
ros2 topic list | grep Power
# 没有输出

# 4. 检查驱动
ros2 node list
# 发现驱动未启动

# 5. 启动驱动
ros2 launch turn_on_wheeltec_robot turn_on_wheeltec_robot.launch.py ...

# 6. 重新测试
ros2 topic echo /PowerVoltage --once
# 成功输出：data: 12.5
```

---

## 总结

本文档提供了所有42个函数的终端命令映射，帮助用户：

1. ✅ 在终端独立测试每个命令
2. ✅ 理解函数的底层实现原理
3. ✅ 快速排查和定位问题
4. ✅ 验证Python代码的正确性
5. ✅ 学习ROS2命令的使用

**文档版本**: v1.0  
**创建日期**: 2026-02-03  
**适用平台**: ROS2 Humble + Wheeltec机器人
