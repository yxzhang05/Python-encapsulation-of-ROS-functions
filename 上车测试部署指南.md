# 上车测试部署指南

本文档详细说明如何将封装好的Python文件部署到ROS机器人系统，并进行实际测试。

---

## 目录

1. [系统环境要求](#1-系统环境要求)
2. [文件部署位置](#2-文件部署位置)
3. [部署步骤](#3-部署步骤)
4. [测试步骤](#4-测试步骤)
5. [常见问题处理](#5-常见问题处理)
6. [安全注意事项](#6-安全注意事项)

---

## 1. 系统环境要求

### 1.1 ROS2系统

**必需条件**：
- ROS2版本：Humble Hawksbill（推荐）
- 操作系统：Ubuntu 22.04
- Python版本：Python 3.10+

**验证方法**：
```bash
# 检查ROS2版本
ros2 --version

# 检查Python版本
python3 --version

# 检查ROS2环境是否正确配置
echo $ROS_DISTRO
```

### 1.2 必要的ROS2包

确保以下ROS2功能包已安装并正常运行：

```bash
# 检查底盘驱动节点
ros2 node list | grep wheeltec

# 检查话题列表
ros2 topic list

# 应该能看到以下话题：
# /cmd_vel          - 速度控制
# /odom             - 里程计数据
# /imu/data_raw     - IMU数据
# /PowerVoltage     - 电池电压
```

---

## 2. 文件部署位置

### 2.1 推荐的部署目录

**方案一：用户主目录（推荐）**

```bash
# 在机器人上创建专用目录
mkdir -p ~/wheeltec_robot_python
cd ~/wheeltec_robot_python
```

**优点**：
- 不需要root权限
- 便于管理和修改
- 不影响ROS系统文件

**方案二：ROS工作空间**

```bash
# 使用ROS工作空间
cd ~/wheeltec_ros2/src
mkdir -p wheeltec_robot_python
cd wheeltec_robot_python
```

**优点**：
- 与ROS项目集成
- 便于版本控制

### 2.2 需要部署的文件

根据测试需求选择合适的库文件：

| 文件名 | 功能范围 | 推荐场景 |
|--------|---------|---------|
| `robot_lib_system.py` | 仅系统管理 | 基础测试 |
| `robot_lib_system_chassis.py` | 系统+底盘 | 运动测试 |
| `robot_lib_system_chassis_arm.py` | +机械臂 | 含机械臂的机器人 |
| `robot_lib_system_chassis_arm_sensors.py` | +感知 | 含传感器的测试 |
| `robot_lib_full.py` | 完整功能 | 全功能测试 |

**建议**：
- 初次测试建议使用 `robot_lib_system_chassis.py`
- 全功能测试使用 `robot_lib_full.py`
- 根据机器人配置选择合适的版本

---

## 3. 部署步骤

### 3.1 通过SSH部署（推荐）

**步骤1：连接到机器人**

```bash
# 替换为机器人的实际IP地址和用户名
ssh wheeltec@192.168.1.100
```

**步骤2：创建工作目录**

```bash
# 创建Python封装工作目录
mkdir -p ~/wheeltec_robot_python
cd ~/wheeltec_robot_python
```

**步骤3：从开发机复制文件**

在**开发机**上执行：

```bash
# 进入项目目录
cd /path/to/Python-encapsulation-of-ROS-functions

# 复制所需的库文件（选择一个或多个）
scp robot_lib_full.py wheeltec@192.168.1.100:~/wheeltec_robot_python/

# 或者复制所有库文件
scp robot_lib*.py wheeltec@192.168.1.100:~/wheeltec_robot_python/
```

**步骤4：设置文件权限**

在**机器人**上执行：

```bash
cd ~/wheeltec_robot_python
chmod +x robot_lib*.py
```

### 3.2 通过U盘部署

**步骤1：在开发机上准备文件**

```bash
# 将文件复制到U盘
cp robot_lib_full.py /media/username/USB_DRIVE/
```

**步骤2：在机器人上复制文件**

```bash
# 挂载U盘（如果未自动挂载）
# 通常自动挂载在 /media/wheeltec/

# 复制文件到工作目录
mkdir -p ~/wheeltec_robot_python
cp /media/wheeltec/*/robot_lib*.py ~/wheeltec_robot_python/
cd ~/wheeltec_robot_python
```

### 3.3 验证部署

```bash
# 检查文件是否正确复制
cd ~/wheeltec_robot_python
ls -lh robot_lib*.py

# 检查Python能否导入（不运行）
python3 -c "import robot_lib_full"
```

如果没有报错，说明部署成功。

---

## 4. 测试步骤

### 4.1 测试前准备

**重要安全检查**：

1. ✅ 确保机器人放置在安全、空旷的区域
2. ✅ 确保周围没有障碍物和人员
3. ✅ 机器人轮子离地（首次测试）或有足够活动空间
4. ✅ 电池电量充足（建议>50%）
5. ✅ 准备好急停按钮或可以快速断电

**启动ROS系统**：

```bash
# 方法1：使用启动脚本
cd ~/wheeltec_ros2
./start.sh

# 方法2：手动启动
source ~/wheeltec_ros2/install/setup.bash
ros2 launch turn_on_wheeltec_robot turn_on_wheeltec_robot.launch.py
```

**等待2-3秒让ROS节点完全启动**

**验证ROS节点运行**：

```bash
# 新开一个终端，检查节点
ros2 node list

# 应该看到类似：
# /wheeltec_robot
# /base_driver
# 等节点
```

### 4.2 基础功能测试

创建测试脚本 `test_basic.py`：

```python
#!/usr/bin/env python3
"""
基础功能测试脚本
测试：初始化、电压读取、速度控制
"""

import sys
sys.path.append('/home/wheeltec/wheeltec_robot_python')
from robot_lib_full import Robot
import time

def main():
    print("=" * 50)
    print("基础功能测试")
    print("=" * 50)
    
    # 1. 初始化机器人（根据实际车型修改：akm, diff, mec）
    print("\n[测试1] 初始化机器人...")
    robot = Robot()
    robot.initialize("diff")  # 差速车型，根据实际情况修改
    print("✓ 初始化成功")
    time.sleep(1)
    
    # 2. 读取电池电压
    print("\n[测试2] 读取电池电压...")
    voltage = robot.get_battery_voltage()
    print(f"✓ 当前电压: {voltage}V")
    
    if voltage < 10.0:
        print("⚠ 警告：电池电压过低，建议充电后再测试")
        robot.shutdown()
        return
    
    # 3. 测试速度控制（非常小的速度，安全测试）
    print("\n[测试3] 速度控制测试...")
    print("  设置速度: 0.1 m/s 前进，持续1秒")
    robot.set_velocity(0.1, 0.0, 0.0)
    time.sleep(1)
    
    print("  停止机器人")
    robot.set_velocity(0.0, 0.0, 0.0)
    time.sleep(0.5)
    
    print("✓ 速度控制正常")
    
    # 4. 读取里程计
    print("\n[测试4] 读取里程计数据...")
    odom = robot._get_odom()
    if odom:
        print(f"✓ 位置: x={odom['x']:.3f}, y={odom['y']:.3f}")
        print(f"  姿态: yaw={odom['yaw']:.3f} rad")
        print(f"  速度: vx={odom['vx']:.3f}, vy={odom['vy']:.3f}, vz={odom['vz']:.3f}")
    
    # 5. 关闭
    print("\n[测试5] 关闭机器人...")
    robot.shutdown()
    print("✓ 测试完成")
    
    print("\n" + "=" * 50)
    print("基础功能测试 - 全部通过 ✓")
    print("=" * 50)

if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        print("\n测试被用户中断")
    except Exception as e:
        print(f"\n测试出错: {e}")
        import traceback
        traceback.print_exc()
```

**运行测试**：

```bash
cd ~/wheeltec_robot_python
python3 test_basic.py
```

### 4.3 运动控制测试

**⚠️ 警告：此测试会让机器人移动，确保空间安全！**

创建测试脚本 `test_motion.py`：

```python
#!/usr/bin/env python3
"""
运动控制测试脚本
测试：直线移动、旋转
"""

import sys
sys.path.append('/home/wheeltec/wheeltec_robot_python')
from robot_lib_full import Robot
import time

def main():
    print("=" * 50)
    print("运动控制测试")
    print("⚠️  确保机器人周围2米内无障碍物")
    print("=" * 50)
    
    input("\n按Enter键开始测试...")
    
    # 初始化
    robot = Robot()
    robot.initialize("diff")  # 根据实际车型修改
    time.sleep(1)
    
    # 测试1：小距离前进
    print("\n[测试1] 前进0.3米...")
    robot.move_distance(0.3, 0.2)  # 0.3米，速度0.2m/s
    print("✓ 前进完成")
    time.sleep(1)
    
    # 测试2：原地旋转
    print("\n[测试2] 原地旋转90度...")
    robot.rotate_angle(90, 0.3)  # 90度，角速度0.3rad/s
    print("✓ 旋转完成")
    time.sleep(1)
    
    # 测试3：后退返回
    print("\n[测试3] 后退0.3米...")
    robot.move_distance(-0.3, 0.2)  # 后退
    print("✓ 后退完成")
    
    # 关闭
    robot.shutdown()
    print("\n运动控制测试 - 全部通过 ✓")

if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        print("\n测试被用户中断")
        robot = Robot()
        robot.emergency_stop()
    except Exception as e:
        print(f"\n测试出错: {e}")
        import traceback
        traceback.print_exc()
```

**运行测试**：

```bash
cd ~/wheeltec_robot_python
python3 test_motion.py
```

### 4.4 传感器测试

创建测试脚本 `test_sensors.py`：

```python
#!/usr/bin/env python3
"""
传感器测试脚本
测试：IMU、雷达、摄像头
"""

import sys
sys.path.append('/home/wheeltec/wheeltec_robot_python')
from robot_lib_full import Robot
import time

def main():
    print("=" * 50)
    print("传感器测试")
    print("=" * 50)
    
    robot = Robot()
    robot.initialize("diff")
    time.sleep(1)
    
    # 测试IMU
    print("\n[测试1] IMU数据...")
    imu_data = robot.get_imu_data()
    if imu_data:
        print(f"✓ 陀螺仪: gx={imu_data['gx']:.3f}, gy={imu_data['gy']:.3f}, gz={imu_data['gz']:.3f}")
        print(f"  加速度: ax={imu_data['ax']:.3f}, ay={imu_data['ay']:.3f}, az={imu_data['az']:.3f}")
    
    # 测试雷达（如果有）
    print("\n[测试2] 启动雷达...")
    robot.launch_lidar()
    time.sleep(2)
    
    print("  读取雷达数据...")
    distances = robot.get_lidar_distance()
    if distances:
        print(f"✓ 前方距离: {distances.get('front', 'N/A')}m")
        print(f"  左侧距离: {distances.get('left', 'N/A')}m")
        print(f"  右侧距离: {distances.get('right', 'N/A')}m")
    
    robot.stop_lidar()
    
    # 测试摄像头（如果有）
    print("\n[测试3] 启动摄像头...")
    robot.launch_camera()
    time.sleep(2)
    print("✓ 摄像头已启动（请检查视频流）")
    
    time.sleep(3)
    robot.stop_camera()
    
    robot.shutdown()
    print("\n传感器测试 - 完成 ✓")

if __name__ == "__main__":
    try:
        main()
    except Exception as e:
        print(f"\n测试出错: {e}")
        import traceback
        traceback.print_exc()
```

### 4.5 综合测试

创建综合测试脚本 `test_full.py`：

```python
#!/usr/bin/env python3
"""
综合功能测试
测试完整的机器人功能流程
"""

import sys
sys.path.append('/home/wheeltec/wheeltec_robot_python')
from robot_lib_full import Robot
import time

def main():
    print("=" * 60)
    print("综合功能测试 - 简单巡逻演示")
    print("=" * 60)
    
    input("\n⚠️  确保机器人周围3米内无障碍物！\n按Enter键开始...")
    
    robot = Robot()
    
    # 1. 初始化
    print("\n[步骤1] 初始化机器人...")
    robot.initialize("diff")
    time.sleep(1)
    
    # 2. 检查电压
    print("\n[步骤2] 检查电池...")
    voltage = robot.get_battery_voltage()
    print(f"电池电压: {voltage}V")
    if voltage < 10.5:
        print("⚠️ 电压过低，停止测试")
        return
    
    # 3. 启动传感器
    print("\n[步骤3] 启动传感器...")
    robot.launch_lidar()
    time.sleep(2)
    
    # 4. 执行巡逻
    print("\n[步骤4] 开始巡逻...")
    for i in range(4):
        print(f"\n  巡逻 {i+1}/4:")
        
        # 前进
        print("    - 前进0.5米")
        robot.move_distance(0.5, 0.2)
        time.sleep(0.5)
        
        # 转向
        print("    - 转向90度")
        robot.rotate_angle(90, 0.3)
        time.sleep(0.5)
        
        # 读取位置
        odom = robot._get_odom()
        if odom:
            print(f"    - 当前位置: ({odom['x']:.2f}, {odom['y']:.2f})")
    
    # 5. 返回起点
    print("\n[步骤5] 返回起点...")
    robot.move_distance(0.5, 0.2)
    
    # 6. 清理
    print("\n[步骤6] 关闭系统...")
    robot.stop_lidar()
    robot.shutdown()
    
    print("\n" + "=" * 60)
    print("综合测试完成 ✓")
    print("=" * 60)

if __name__ == "__main__":
    try:
        main()
    except KeyboardInterrupt:
        print("\n测试被用户中断")
        robot = Robot()
        robot.emergency_stop()
    except Exception as e:
        print(f"\n测试出错: {e}")
        import traceback
        traceback.print_exc()
        robot = Robot()
        robot.emergency_stop()
```

---

## 5. 常见问题处理

### 5.1 "无法导入模块"错误

**错误信息**：
```
ModuleNotFoundError: No module named 'robot_lib_full'
```

**解决方法**：
```bash
# 方法1：将文件复制到当前目录
cp ~/wheeltec_robot_python/robot_lib_full.py ./

# 方法2：添加路径到Python
export PYTHONPATH=$PYTHONPATH:~/wheeltec_robot_python
```

### 5.2 "话题不存在"错误

**错误信息**：
```
Failed to get topic data
```

**检查步骤**：
```bash
# 1. 确认ROS节点运行
ros2 node list

# 2. 确认话题存在
ros2 topic list

# 3. 测试话题数据
ros2 topic echo /odom --once

# 4. 如果话题不存在，重启ROS驱动
cd ~/wheeltec_ros2
./start.sh
```

### 5.3 串口权限问题

**错误信息**：
```
Permission denied: '/dev/ttyUSB0'
```

**解决方法**：
```bash
# 添加用户到dialout组
sudo usermod -a -G dialout $USER

# 或临时设置权限
sudo chmod 666 /dev/ttyUSB0

# 重新登录或重启以生效
```

### 5.4 机器人不响应运动命令

**检查步骤**：

1. **检查速度话题**：
```bash
ros2 topic echo /cmd_vel
# 执行Python命令后应该看到速度消息
```

2. **检查底盘驱动节点**：
```bash
ros2 node info /wheeltec_robot
# 应该显示订阅了 /cmd_vel
```

3. **手动测试速度控制**：
```bash
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.1}, angular: {z: 0.0}}" --once
# 机器人应该短暂移动
```

### 5.5 急停功能不工作

**紧急停止方法**：

1. **软件急停**：
```python
robot.emergency_stop()
```

2. **命令行急停**：
```bash
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.0}, angular: {z: 0.0}}" --once
```

3. **硬件急停**：
- 按下机器人上的物理急停按钮
- 或直接断开电源

### 5.6 IMU或雷达数据读取失败

**检查硬件连接**：
```bash
# 检查IMU设备
ls -l /dev/tty* | grep USB

# 检查雷达设备
ls -l /dev/lidar* 
ls -l /dev/ttyUSB*

# 重启设备节点
ros2 launch wheeltec_lidar_ros2 wheeltec_lidar.launch.py
```

---

## 6. 安全注意事项

### 6.1 测试前必须检查

- ✅ **空间安全**：确保3米内无人员和障碍物
- ✅ **电量充足**：电池电压 > 11.0V
- ✅ **急停可用**：确认急停按钮功能正常
- ✅ **通信正常**：ROS话题通信正常
- ✅ **地面平坦**：避免在坡道或不平整地面测试

### 6.2 测试时注意事项

1. **首次测试**：
   - 建议将机器人架起（轮子离地）
   - 或使用非常小的速度（0.1 m/s）

2. **逐步增加难度**：
   - 先测试基础功能
   - 再测试运动控制
   - 最后测试综合功能

3. **随时准备急停**：
   - 一手放在键盘上（Ctrl+C）
   - 或一手放在急停按钮上

4. **异常立即停止**：
   - 发现异常响声
   - 运动轨迹异常
   - 通信延迟严重
   - 立即执行急停

### 6.3 不要在以下情况测试

- ❌ 电池电压低于10.5V
- ❌ 室外有风、雨、雪天气
- ❌ 地面有水、油污
- ❌ 周围有人员活动
- ❌ 空间狭小、有贵重物品

### 6.4 紧急情况处理

**机器人失控时**：
1. 按下物理急停按钮
2. 或立即断开电源
3. 不要试图用手阻止机器人

**软件卡死时**：
1. Ctrl+C 终止Python程序
2. `ros2 topic pub /cmd_vel ...` 发送零速度
3. 重启ROS驱动节点

---

## 7. 测试记录模板

建议在测试时填写以下记录：

```
测试日期：__________
测试人员：__________
机器人型号：________
ROS版本：__________

测试项目         | 结果 | 备注
---------------|------|------
1. 基础功能测试   | [ ] | 
2. 运动控制测试   | [ ] | 
3. 传感器测试     | [ ] | 
4. 综合功能测试   | [ ] | 

遇到的问题：
1. 
2. 
3. 

改进建议：
1. 
2. 
3. 
```

---

## 8. 进阶使用

### 8.1 开机自动加载

如果测试成功，可以设置开机自动加载：

```bash
# 编辑 .bashrc
nano ~/.bashrc

# 添加以下内容到文件末尾
export PYTHONPATH=$PYTHONPATH:~/wheeltec_robot_python
alias robot_test='python3 ~/wheeltec_robot_python/test_basic.py'
```

### 8.2 创建systemd服务

创建系统服务自动运行Python程序：

```bash
# 创建服务文件
sudo nano /etc/systemd/system/wheeltec_python.service

# 内容：
[Unit]
Description=Wheeltec Robot Python Service
After=network.target

[Service]
Type=simple
User=wheeltec
WorkingDirectory=/home/wheeltec/wheeltec_robot_python
ExecStart=/usr/bin/python3 /home/wheeltec/wheeltec_robot_python/your_app.py
Restart=on-failure

[Install]
WantedBy=multi-user.target

# 启用服务
sudo systemctl enable wheeltec_python.service
sudo systemctl start wheeltec_python.service
```

### 8.3 远程监控

使用SSH远程监控机器人状态：

```bash
# 在开发机上
ssh wheeltec@192.168.1.100

# 实时查看Python程序输出
tail -f ~/wheeltec_robot_python/test.log
```

---

## 9. 总结

### 9.1 完整部署流程

```
1. 准备 → 检查ROS环境
    ↓
2. 复制 → 将Python文件复制到机器人
    ↓
3. 验证 → 测试导入和基础功能
    ↓
4. 测试 → 逐步测试各项功能
    ↓
5. 调试 → 解决遇到的问题
    ↓
6. 应用 → 开发实际应用程序
```

### 9.2 快速参考命令

```bash
# 部署
scp robot_lib_full.py wheeltec@IP:~/wheeltec_robot_python/

# 测试
ssh wheeltec@IP
cd ~/wheeltec_robot_python
python3 test_basic.py

# 监控
ros2 topic list
ros2 topic echo /cmd_vel
ros2 node list

# 急停
ros2 topic pub /cmd_vel geometry_msgs/msg/Twist "{linear: {x: 0.0}}" --once
```

### 9.3 技术支持

如遇到问题：

1. 查看本文档"常见问题处理"章节
2. 查看 `算法一致性分析.md` 了解实现原理
3. 查看 `使用说明.md` 了解API详情
4. 查看ROS日志：`ros2 topic list` 和 `ros2 node info`

---

**文档版本**: v1.0  
**更新日期**: 2026-02-03  
**适用范围**: Wheeltec ROS2机器人系列

**祝测试顺利！**
