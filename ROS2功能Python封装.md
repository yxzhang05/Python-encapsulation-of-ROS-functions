# ROS封装规划

**函数封装修改原则**

1.  不同级别，单独电机-整体运动状态-整体位姿
2.  阻塞/非阻塞说明，
3.  上位机能获取到的信息都有函数读的出来，部分函数可否采用多线程+回调函数的方法增加订阅机制

1.  系统管理

①**initialize(robot_type)**

- **传参**: robot_type (str) - 车型枚举，例如 "akm", "diff", "mec"。
- **解释**: 初始化机器人系统。这应该在后台调用 roslaunch 启动对应的串口驱动、TF变换和底盘控制器。成功后返回True。

②**shutdown()**

- **传参**: 无。
- **解释**: 安全关闭机器人。停止所有电机运动，关闭雷达/相机流，kill掉后台的ROS子进程。

③**get_battery_voltage()**

- **传参**: 无。
- **解释**: 获取当前底盘电池电压，用于低电量报警逻辑。

④**emergency_stop()**

- **传参**: 无。
- **解释**: 软件急停。立即发送全0速度指令

⑤**Get_software_version**()

- 获取下位机软件或固件的版本号，便于后续版本更新甄别

1.  底盘运动控制

①**set_velocity(x,x,x)**

- **传参**:
    - v_x (float): 线速度，单位 m/s (前进为正)。
    - v_y (float): 横向速度，单位 m/s (仅麦轮有效，其他车型传0)。
    - w_z (float): 角速度，单位 rad/s (左转为正)。

尽可能兼容3种车型，看怎么设计合适

- **解释**: 最底层的控制接口，对应ROS的 /cmd_vel。

②**move_distance(distance, speed)**

- **传参**:
    - distance (float): 移动距离，单位米（负数代表后退）。
    - speed (float): 移动速度，单位 m/s (必须为正数)。

1.  move_distance分为麦轮与阿克曼/差速全向轮、麦轮两个函数
    
    1.  阿克曼/差速全向可以设置转弯半径与线速度、移动距离
    
    - 麦轮可以设置x、y方向的移动距离与速度

- **解释**: 闭环控制小车前进或后退指定距离。函数内部需要读取里程计(Odom)反馈来判断是否到达。

③**rotate_angle(angle, speed)**

- **传参**:
    - angle (float): 旋转角度，单位度 (Degrees)。
    - speed (float): 旋转角速度，单位 rad/s。
- **解释**: 原地旋转（麦轮/差速）或行进间转向（阿克曼需特殊处理，可能需要配合微小线速度）指定角度。

**④获取四轮实时速度 get_xxx()**

**⑤获取陀螺仪6轴信息 get_xxx()**

**⑥获取车身位姿get_xxx()**

**⑦直接设置4个轮子的速度set_xxx(),如set_xxx(50,50,50,50)**

**⑧设置阿克曼转向角度**

1.  机械臂控制

①**arm_home()**

**传参**: 无。

**解释**: 机械臂复位，回到初始姿态。

②**set_joint_angles(joint1, joint2)**

- **传参**:
    - joint1 (float): 大臂（靠近底座的关节）的目标角度，单位度。
    - joint2 (float): 小臂（第二段关节）的目标角度，单位度。
- **解释**: 直接给底层的电机发送角度指令。

③**set_yaw_angle(angle)**

- **传参**:
    - angle (float): 云台/独立关节的角度，单位度。
- **解释**: 控制那个独立的旋转自由度（云台），通常用于调整末端朝向或挂载摄像头的角度。

④**set_arm_position(x, y)**

- **传参**:
    - x (float): 目标点X坐标（相对于机械臂底座），单位 mm。
    - y (float): 目标点Y坐标，单位 mm。
- **解释**: 调用内部的逆运动学解算（IK），控制机械臂末端移动到平面内的指定坐标。

⑤**set_gripper(x)**

- **传参**: 根据数值控制夹爪的开合程度，0-10
- **解释**: 控制机械臂末端夹爪张开或闭合。

⑥**get_arm_pose_xy()**

**传参**: 无。

**解释**: 获取当前机械臂末端的坐标。

**返回值**: 元组 (x, y)，单位 mm。

⑦**get_joint_states()**

**传参**: 无。

**解释**: 获取当前所有关节的实时角度值。

**返回值**: 包含3个角度的列表/元组 (joint1_angle, joint2_angle, yaw_angle)

**⑧set_pwm_xxx()**

设置主控板的某个IO引脚的占空比

1.  感知与功能 (Sensors & Applications)

①**launch_lidar()**

**传参**: 无。

**解释**: 启动雷达的ROS驱动节点。启动后，开始发布 /scan 话题数据，自动打开rviz可视化界面

②**stop_lidar()**

**传参**: 无。

**解释**: 关闭雷达驱动节点，停止发布数据。

③**launch_camera()**

**传参**: 无。

**解释**: 启动 USB 相机或深度相机的驱动节点。启动后系统开始采集图像并发布 /camera/image_raw 或类似话题。自动打开可视化界面

④**stop_camera()**

- **传参**: 无。
- **解释**: 关闭相机驱动节点。

⑤**start_visual_follow(color)**

- **传参**:
    - color (str): 目标颜色。字符串，如 'red', 'blue', 'green', 'yellow'。底层代码需要预设这些颜色的 HSV 阈值。
- **解释**: 启动视觉跟随算法。机器人会在视野中寻找指定颜色的物体，并计算其中心点偏差，自动调整线速度和角速度以保持物体在画面中心并保持一定距离。

⑥**start_line_tracking(color) （这里可以用机械臂相机实现）**

- **传参**:
    - color (str): 巡线线条的颜色。通常为 'black' (白底黑线) 或 'red', 'yellow' (特定胶带)。
- **解释**: 启动视觉巡线算法。机器人通过处理图像识别线条的曲率和偏差，自动控制底盘沿线行驶。

⑦**start_lidar_follow(target_dist)**

- **传参**:
    - target_dist (float): 保持的跟随距离，单位米。
- **解释**: 启动雷达跟随。通常是通过过滤雷达数据，锁定正前方最近的移动障碍物（人）进行跟随。

⑧**stop_application()**

- **传参**: 无。
- **解释**: 停止当前的跟随或巡线任务。**注**：该函数不仅要停止算法节点，还必须发送一次全 0 的速度指令让小车立刻停下来，释放底盘控制权。

⑨获取相机当前的一帧图()=拍照

⑩获取雷达某个角度的距离信息

**建议增加的函数**

1.  增加两个函数，调用时分别返回start_visual_follow、start_line_tracking两个函数搜索的目标信息（指定颜色目标信息、循迹线信息）。**这条功能也可以考虑通过start_visual_follow、start_line_tracking函数中增加回调函数实现，但需要给这两个函数增加一个不进行底盘控制的参数**
2.  增加start_visual_follow、start_line_tracking函数使用自定义颜色的方法

1.  建图与导航(SLAM & Navigation )

①**start_mapping(method)**

**传参**: method (str) - 建图算法，默认 "gmapping" 或 "cartographer"。

**解释**: 启动SLAM建图节点，此时可以通过遥控或键盘控制小车移动来建图。

②**save_map(map_name)**

- **传参**: map_name (str) - 保存地图的文件名。
- **解释**: 调用 map_server 的 map_saver 功能，保存当前构建的地图。

③**start_keyboard_control()**

- **传参**: 无。
- **解释**: 启动键盘控制节点（Teleop）。允许用户通过键盘（如WASD或方向键）手动控制小车移动，常用于辅助建图。

④**move_to_goal(x, y, theta)**

**传参**: x, y (m), theta (度)。

**解释**: 发布导航目标点，机器人自动规划路径前往。

⑥**cancel_navigation()**

- **传参**: 无。
- **解释**: 取消当前正在进行的导航任务。

**问题**

1.  **建图与导航相关函数是在单独的线程中完成的吗，是否是非阻塞的？**例如我可否在一段python程序中用start_mapping启动建图后，再读取激光雷达数据，并使用底盘控制函数控制小车行驶，进行自动建图；在达到某些条件后运行save_map自动保存地图并退出程序
2.  **可否增加读取建图和导航中各类状态信息的函数。**例如建图过程中已经建好的地图，导航过程中机器人的状态（到达目标，正在前往目标，或者通过）

**这条功能看看可否通过在start_mapping、move_to_goal等函数中增加回调函数实现**