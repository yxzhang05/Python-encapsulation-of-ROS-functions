# 视觉应用修复说明

## 问题总结

用户反馈了三个关键问题：

1. **错误的Launch命令** - 包名和文件名都错误
2. **颜色参数无法传入** - 参数传递方式不正确
3. **stop_application无法完全停止** - 进程终止不完整

---

## 源码分析

### 正确的Launch命令

通过检查 `src/simple_follower_ros2/launch/` 目录，发现正确的launch文件：

```bash
# 视觉巡线
ros2 launch simple_follower_ros2 line_follower.launch.py

# 视觉跟随
ros2 launch simple_follower_ros2 visual_follower.launch.py

# 雷达跟随
ros2 launch simple_follower_ros2 laser_follower.launch.py
```

**关键发现**:
- 包名: `simple_follower_ros2` (不是 wheeltec_robot_kcf)
- 文件名: `line_follower.launch.py` (不是 line_tracking.launch.py)
- 文件名: `visual_follower.launch.py` (不是 visual_follow.launch.py)
- 文件名: `laser_follower.launch.py` (不是 lidar_follow.launch.py)

### 颜色参数真相

通过分析 `src/simple_follower_ros2/simple_follower_ros2/line_follow.py` 源码：

```python
# 颜色定义
col_black = (0,0,0,180,255,46)
col_red = (0,100,80,10,255,255)
col_blue = (100,43,46,124,255,255)
col_green= (35,43,46,77,255,255)
col_yellow = (26,43,46,34,255,255)

# 通过trackbar选择颜色
cv2.createTrackbar(Switch,'Adjust_hsv',0,4,nothing)
m=cv2.getTrackbarPos(Switch,'Adjust_hsv')
```

**关键发现**:
- 颜色不是通过launch参数传入的
- 颜色通过OpenCV的trackbar在运行时选择
- 用户需要在GUI窗口中手动选择颜色

---

## 修复方案

### 1. start_visual_follow()

**修复前**:
```python
def start_visual_follow(self, color):
    cmd = ["ros2", "launch", "wheeltec_robot_kcf", "visual_follow.launch.py", 
           f"target_color:={color}"]
```

**修复后**:
```python
def start_visual_follow(self):
    print("[Info] 程序启动后，请在弹出的窗口中选择目标颜色")
    cmd = ["ros2", "launch", "simple_follower_ros2", "visual_follower.launch.py"]
    # 使用os.setsid创建进程组，方便后续终止
    subprocess.Popen(cmd, preexec_fn=os.setsid, ...)
```

### 2. start_line_tracking()

**修复前**:
```python
def start_line_tracking(self, color):
    cmd = ["ros2", "launch", "wheeltec_robot_kcf", "line_tracking.launch.py",
           f"line_color:={color}"]
```

**修复后**:
```python
def start_line_tracking(self):
    print("[Info] 程序启动后，请在'Adjust_hsv'窗口中选择线条颜色")
    print("[Info] 0:Red, 1:Green, 2:Blue, 3:Yellow, 4:Black")
    cmd = ["ros2", "launch", "simple_follower_ros2", "line_follower.launch.py"]
    subprocess.Popen(cmd, preexec_fn=os.setsid, ...)
```

### 3. start_lidar_follow()

**修复前**:
```python
def start_lidar_follow(self, target_dist=0.8):
    cmd = ["ros2", "launch", "simple_follower_ros2", "lidar_follow.launch.py",
           f"target_distance:={target_dist}"]
```

**修复后**:
```python
def start_lidar_follow(self):
    cmd = ["ros2", "launch", "simple_follower_ros2", "laser_follower.launch.py"]
    subprocess.Popen(cmd, preexec_fn=os.setsid, ...)
```

### 4. stop_application()

**修复前**:
```python
def stop_application(self):
    self.application_process.send_signal(signal.SIGINT)
    # 只发送SIGINT，可能无法完全停止
```

**修复后**:
```python
def stop_application(self):
    # 获取进程组ID
    pgid = os.getpgid(self.application_process.pid)
    
    # 多级终止策略
    os.killpg(pgid, signal.SIGINT)   # 1. 优雅退出
    time.sleep(2)
    
    if process.poll() is None:
        os.killpg(pgid, signal.SIGTERM)  # 2. 强制终止
        time.sleep(1.5)
    
    if process.poll() is None:
        os.killpg(pgid, signal.SIGKILL)  # 3. 立即终止
```

---

## 使用指南

### 视觉巡线

```python
from robot_lib_full import Robot

robot = Robot()
robot.initialize("mec")

# 启动视觉巡线
robot.start_line_tracking()  # 不需要传颜色参数

# 在弹出的'Adjust_hsv'窗口中：
# - 拖动trackbar选择颜色（0-4）
# - 0:Red, 1:Green, 2:Blue, 3:Yellow, 4:Black

time.sleep(30)  # 运行30秒

# 停止
robot.stop_application()  # 现在能完全停止
```

### 视觉跟随

```python
# 启动视觉跟随
robot.start_visual_follow()  # 不需要传颜色参数

# 在弹出的窗口中选择目标颜色
# （具体操作取决于程序GUI）

# 停止
robot.stop_application()
```

### 雷达跟随

```python
# 启动雷达跟随
robot.start_lidar_follow()  # 不需要传距离参数

# 机器人会自动跟随前方障碍物

# 停止
robot.stop_application()
```

---

## 迁移指南

### 破坏性变更

**函数签名改变**:
- `start_visual_follow(color)` → `start_visual_follow()`
- `start_line_tracking(color)` → `start_line_tracking()`
- `start_lidar_follow(target_dist)` → `start_lidar_follow()`

### 如何迁移

**旧代码**:
```python
robot.start_line_tracking("black")
robot.start_visual_follow("red")
robot.start_lidar_follow(0.5)
```

**新代码**:
```python
robot.start_line_tracking()  # 启动后在GUI选择黑色
robot.start_visual_follow()  # 启动后在GUI选择红色
robot.start_lidar_follow()   # 使用默认参数
```

---

## 技术细节

### 进程组管理

**为什么需要进程组？**

launch文件会启动多个节点：
- wheeltec_robot (底盘驱动)
- wheeltec_camera (相机驱动)
- line_follow (巡线节点)

如果只终止主进程，子进程会继续运行。

**解决方案**:
```python
# 1. 启动时创建进程组
subprocess.Popen(..., preexec_fn=os.setsid)

# 2. 停止时终止整个进程组
pgid = os.getpgid(process.pid)
os.killpg(pgid, signal.SIGINT)
```

### 多级终止策略

```
SIGINT (信号2)
  优雅退出，允许程序清理资源
  等待2秒
  ↓
SIGTERM (信号15)
  强制终止，要求立即退出
  等待1.5秒
  ↓
SIGKILL (信号9)
  立即终止，操作系统强制终止
  无法被程序捕获
```

---

## 故障排查

### 问题1: 启动失败

**症状**: `[Error] 视觉巡线启动失败`

**排查**:
```bash
# 1. 检查包是否存在
ros2 pkg list | grep simple_follower

# 2. 检查launch文件
ros2 pkg prefix simple_follower_ros2

# 3. 手动测试launch
ros2 launch simple_follower_ros2 line_follower.launch.py
```

### 问题2: stop_application仍无法停止

**症状**: 调用stop_application后，程序仍在运行

**排查**:
```bash
# 1. 检查ROS节点
ros2 node list

# 2. 手动终止节点
ros2 lifecycle set /node_name shutdown
```

### 问题3: 找不到颜色选择界面

**症状**: 启动后没有看到GUI窗口

**原因**: 可能在远程SSH会话中运行

**解决**:
```bash
# 1. 确保有X11转发
ssh -X user@robot

# 2. 或者在机器人本地终端运行
# 3. 或者使用VNC连接到机器人桌面
```

---

## 总结

### 修复要点

1. ✅ 使用正确的包名: `simple_follower_ros2`
2. ✅ 使用正确的文件名: `*_follower.launch.py`
3. ✅ 移除无效的参数
4. ✅ 在启动后通过GUI选择颜色
5. ✅ 使用进程组终止
6. ✅ 多级终止策略确保完全停止

### 用户体验改进

1. ✅ 清晰的提示信息
2. ✅ 完全停止应用
3. ✅ 符合实际工作方式
4. ✅ 易于理解和使用

---

**修复日期**: 2026-02-04  
**修复版本**: v2.4  
**状态**: ✅ 完成
