# ROS2 功能 Python 封装 - 使用说明

## 目录
1. [快速开始](#快速开始)
2. [环境要求](#环境要求)
3. [库文件选择](#库文件选择)
4. [基础使用教程](#基础使用教程)
5. [完整功能示例](#完整功能示例)
6. [常见问题](#常见问题)
7. [API 参考](#api-参考)

---

## 快速开始

### 最简单的示例（系统管理）

```python
from robot_lib_system import Robot

# 创建机器人对象
robot = Robot()

# 初始化机器人（麦轮车型）
if robot.initialize("mec"):
    print("初始化成功！")
    
    # 获取电池电压
    voltage = robot.get_battery_voltage()
    print(f"电池电压: {voltage}V")
    
    # 安全关闭
    robot.shutdown()
```

### 运动控制示例

```python
from robot_lib_system_chassis import Robot

robot = Robot()
robot.initialize("mec")

# 前进
robot.set_velocity(0.2, 0.0, 0.0)
import time
time.sleep(3)
robot.set_velocity(0, 0, 0)

# 移动1米
robot.move_distance(1.0, speed=0.3)

# 旋转90度
robot.rotate_angle(90)

robot.shutdown()
```

---

## 环境要求

### 硬件要求
- Wheeltec 机器人底盘（阿克曼/差速/麦轮）
- USB 相机（可选）
- 激光雷达（可选）
- 机械臂（可选）

### 软件要求
- Ubuntu 22.04
- ROS2 Humble
- Python 3.10+
- Wheeltec 功能包已编译

### 检查环境

```bash
# 检查 ROS2 是否安装
ros2 --version

# 检查 Wheeltec 功能包
source ~/wheeltec_ros2/install/setup.bash
ros2 pkg list | grep wheeltec
```

---

## 库文件选择

本项目采用**累积式设计**，每个文件包含前面所有功能：

| 文件名 | 大小 | 包含功能 | 适用场景 |
|--------|------|----------|----------|
| `robot_lib_system.py` | 4.6KB | 系统管理 | 只需基本初始化和关闭 |
| `robot_lib_system_chassis.py` | 15KB | + 底盘运动 | 需要控制机器人移动 |
| `robot_lib_system_chassis_arm.py` | 22KB | + 机械臂 | 带机械臂的机器人 |
| `robot_lib_system_chassis_arm_sensors.py` | 29KB | + 感知功能 | 需要传感器和视觉应用 |
| `robot_lib_full.py` | 35KB | + 建图导航 | 需要所有功能（推荐） |

**选择建议**：
1. 🎯 如果只需要特定功能，选择对应的文件即可
2. 🎯 不确定需要哪些功能时，直接使用 `robot_lib_full.py`
3. 🎯 所有文件的使用方法完全相同，只是包含的函数不同

---

## 基础使用教程

### 1. 系统管理基础

```python
from robot_lib_system import Robot

robot = Robot()

# 初始化（三种车型）
robot.initialize("akm")   # 阿克曼
robot.initialize("diff")  # 差速
robot.initialize("mec")   # 麦轮

# 获取电池电压
voltage = robot.get_battery_voltage()
if voltage < 11.0:
    print("电池电量低，请充电！")

# 紧急停止
robot.emergency_stop()

# 获取版本信息
version = robot.get_software_version()
print(f"软件版本: {version}")

# 安全关闭
robot.shutdown()
```

### 2. 底盘运动控制

```python
from robot_lib_system_chassis import Robot
import time

robot = Robot()
robot.initialize("mec")

# 基本速度控制
robot.set_velocity(0.3, 0.0, 0.0)  # 前进 0.3 m/s
time.sleep(2)
robot.set_velocity(0.0, 0.2, 0.0)  # 左移（仅麦轮）
time.sleep(2)
robot.set_velocity(0, 0, 0)        # 停止

# 精确移动
robot.move_distance(1.5, speed=0.3)  # 前进1.5米

# 麦轮车型的XY移动
robot.move_distance_xy(1.0, 0.5, speed_x=0.3, speed_y=0.2)

# 精确旋转
robot.rotate_angle(90, angular_speed=0.5)   # 逆时针90度
robot.rotate_angle(-45, angular_speed=0.5)  # 顺时针45度

# 获取机器人状态
pose = robot.get_robot_pose()
print(f"位置: x={pose['x']:.2f}, y={pose['y']:.2f}, theta={pose['theta']:.2f}")

# 获取IMU数据
imu = robot.get_imu_data()
print(f"加速度: {imu['accel_x']:.2f}, {imu['accel_y']:.2f}, {imu['accel_z']:.2f}")

robot.shutdown()
```

### 3. 机械臂控制

```python
from robot_lib_system_chassis_arm import Robot

robot = Robot()
robot.initialize("mec")

# 机械臂复位
robot.arm_home()

# 关节角度控制
robot.set_joint_angles(45, -30)  # 大臂45度，小臂-30度

# 末端位置控制（逆运动学）
robot.set_arm_position(250, 150)  # X=250mm, Y=150mm

# 云台控制
robot.set_yaw_angle(30)  # 云台转30度

# 夹爪控制
robot.set_gripper(0)   # 闭合
time.sleep(1)
robot.set_gripper(10)  # 打开

# 获取机械臂状态
x, y = robot.get_arm_pose_xy()
print(f"末端位置: ({x:.1f}, {y:.1f}) mm")

joint1, joint2, yaw = robot.get_joint_states()
print(f"关节角度: {joint1:.1f}°, {joint2:.1f}°, yaw={yaw:.1f}°")

robot.shutdown()
```

### 4. 感知功能

```python
from robot_lib_system_chassis_arm_sensors import Robot
import time

robot = Robot()
robot.initialize("mec")

# 启动雷达
robot.launch_lidar()

# 获取雷达数据
dist_front = robot.get_lidar_distance(0)      # 正前方
dist_left = robot.get_lidar_distance(90)      # 左侧
dist_right = robot.get_lidar_distance(-90)    # 右侧
print(f"前方距离: {dist_front:.2f}m")

# 启动相机
robot.launch_camera()

# 启动视觉跟随
robot.start_visual_follow("red")   # 跟随红色物体
time.sleep(10)
robot.stop_application()

# 启动视觉巡线
robot.start_line_tracking("black")  # 黑线巡线
time.sleep(10)
robot.stop_application()

# 启动雷达跟随
robot.start_lidar_follow(target_dist=0.8)  # 保持0.8米距离
time.sleep(10)
robot.stop_application()

# 停止传感器
robot.stop_camera()
robot.stop_lidar()

robot.shutdown()
```

### 5. 建图与导航

```python
from robot_lib_full import Robot
import time

robot = Robot()
robot.initialize("mec")

# === 第一步：建图 ===
robot.start_mapping("gmapping")  # 或 "cartographer"

# 键盘控制机器人移动，建立地图
print("请使用键盘控制机器人移动，建立完整地图...")
robot.start_keyboard_control()

# 保存地图
robot.save_map("office_map")
print("地图已保存！")

# === 第二步：自主导航 ===
robot.load_map_and_start_navigation("office_map")

# 导航到多个目标点
goals = [
    (2.0, 1.0, 0),      # 位置1
    (3.5, 2.5, 90),     # 位置2
    (1.0, 3.0, 180),    # 位置3
]

for i, (x, y, theta) in enumerate(goals, 1):
    print(f"导航到目标点 {i}: ({x}, {y}, {theta}°)")
    robot.move_to_goal(x, y, theta)
    time.sleep(10)  # 等待导航完成

robot.shutdown()
```

---

## 完整功能示例

### 综合应用：自动巡逻

```python
from robot_lib_full import Robot
import time

def patrol_demo():
    robot = Robot()
    
    try:
        # 初始化
        robot.initialize("mec")
        
        # 启动传感器
        robot.launch_lidar()
        robot.launch_camera()
        
        # 巡逻路径
        patrol_points = [
            (0.0, 0.0, 0),
            (2.0, 0.0, 90),
            (2.0, 2.0, 180),
            (0.0, 2.0, 270),
        ]
        
        # 循环巡逻
        for round in range(3):  # 巡逻3圈
            print(f"\n=== 第 {round+1} 圈巡逻 ===")
            
            for i, (x, y, theta) in enumerate(patrol_points, 1):
                print(f"前往点 {i}: ({x}, {y})")
                
                # 检查前方障碍物
                dist = robot.get_lidar_distance(0)
                if dist < 0.5:
                    print("前方有障碍物，等待...")
                    time.sleep(2)
                    continue
                
                # 移动到目标点
                robot.move_distance(2.0, speed=0.3)
                robot.rotate_angle(90)
                
                # 到达后拍照
                print(f"到达点 {i}，记录环境")
                time.sleep(1)
            
            print(f"第 {round+1} 圈完成！")
        
        print("\n巡逻任务完成！")
        
    finally:
        robot.stop_lidar()
        robot.stop_camera()
        robot.shutdown()

if __name__ == "__main__":
    patrol_demo()
```

---

## 常见问题

### Q1: 初始化失败怎么办？

**A:** 检查以下几点：
1. 确认ROS2环境已source
2. 确认Wheeltec功能包已编译
3. 检查USB串口权限：`sudo chmod 666 /dev/ttyUSB0`
4. 检查硬件连接是否正常

### Q2: 为什么函数调用后没有反应？

**A:** 可能的原因：
1. 硬件未上电
2. ROS话题未正确发布
3. 使用了错误的车型参数
4. 函数调用后需要等待一段时间

### Q3: 如何选择合适的库文件？

**A:** 
- 只需要基本功能：使用 `robot_lib_system.py`
- 需要运动控制：使用 `robot_lib_system_chassis.py`
- 需要完整功能：使用 `robot_lib_full.py`（推荐）

### Q4: 可以同时运行多个应用吗？

**A:** 不建议。每次只运行一个Python程序，避免话题冲突。

### Q5: 如何停止正在运行的程序？

**A:** 按 `Ctrl+C`，程序会自动调用 `shutdown()` 安全退出。

---

## API 参考

### 系统管理

#### initialize(robot_type)
初始化机器人系统
- **参数**：`robot_type` (str) - 车型："akm", "diff", "mec"
- **返回**：bool - 是否成功

#### shutdown()
关闭机器人系统
- **返回**：None

#### get_battery_voltage()
获取电池电压
- **返回**：float - 电压值（伏特）

#### emergency_stop()
紧急停止
- **返回**：None

#### get_software_version()
获取软件版本
- **返回**：str - 版本字符串

---

### 底盘运动控制

#### set_velocity(v_x, v_y=0.0, w_z=0.0)
设置运动速度
- **参数**：
  - `v_x` (float) - X方向速度 (m/s)
  - `v_y` (float) - Y方向速度 (m/s，仅麦轮)
  - `w_z` (float) - 角速度 (rad/s)

#### move_distance(distance, speed=0.3)
移动指定距离（差速/阿克曼）
- **参数**：
  - `distance` (float) - 距离 (m)
  - `speed` (float) - 速度 (m/s)
- **返回**：bool

#### move_distance_xy(distance_x, distance_y, speed_x=0.3, speed_y=0.3)
XY方向移动（麦轮专用）
- **参数**：各方向的距离和速度
- **返回**：bool

#### rotate_angle(angle_degrees, angular_speed=0.5)
旋转指定角度
- **参数**：
  - `angle_degrees` (float) - 角度（度）
  - `angular_speed` (float) - 角速度 (rad/s)
- **返回**：bool

#### get_robot_pose()
获取机器人位姿
- **返回**：dict - {"x", "y", "theta"}

#### get_imu_data()
获取IMU数据
- **返回**：dict - 6轴数据

---

### 机械臂控制

#### set_joint_angles(joint1, joint2)
设置关节角度
- **参数**：
  - `joint1` (float) - 大臂角度（度）
  - `joint2` (float) - 小臂角度（度）
- **返回**：bool

#### set_arm_position(x, y)
设置末端位置（逆运动学）
- **参数**：
  - `x` (float) - X坐标 (mm)
  - `y` (float) - Y坐标 (mm)
- **返回**：bool

#### set_gripper(value)
控制夹爪
- **参数**：`value` (float) - 开度 0-10
- **返回**：bool

#### arm_home()
机械臂复位
- **返回**：bool

---

### 感知功能

#### launch_lidar()
启动雷达
- **返回**：bool

#### stop_lidar()
停止雷达

#### launch_camera()
启动相机
- **返回**：bool

#### stop_camera()
停止相机

#### start_visual_follow(color)
启动视觉跟随
- **参数**：`color` (str) - "red", "blue", "green", "yellow"
- **返回**：bool

#### start_line_tracking(color)
启动视觉巡线
- **参数**：`color` (str) - "black", "red", "yellow", "white"
- **返回**：bool

#### start_lidar_follow(target_dist=0.8)
启动雷达跟随
- **参数**：`target_dist` (float) - 目标距离 (m)
- **返回**：bool

#### stop_application()
停止当前应用

#### get_lidar_distance(angle_degrees=0)
获取指定角度的雷达距离
- **参数**：`angle_degrees` (float) - 角度（度）
- **返回**：float - 距离 (m)

---

### 建图与导航

#### start_mapping(method="gmapping")
启动SLAM建图
- **参数**：`method` (str) - "gmapping" 或 "cartographer"
- **返回**：bool

#### save_map(map_name="my_map")
保存地图
- **参数**：`map_name` (str) - 地图名称
- **返回**：bool

#### load_map_and_start_navigation(map_name)
加载地图并启动导航
- **参数**：`map_name` (str) - 地图名称
- **返回**：bool

#### move_to_goal(x, y, theta=0.0)
导航到目标点
- **参数**：
  - `x` (float) - X坐标 (m)
  - `y` (float) - Y坐标 (m)
  - `theta` (float) - 朝向角度（度）
- **返回**：bool

#### cancel_navigation()
取消当前导航
- **返回**：bool

#### start_keyboard_control()
启动键盘控制（阻塞式）

---

**版本**：v2.0  
**最后更新**：2026-02-02  
**维护者**：yxzhang05
