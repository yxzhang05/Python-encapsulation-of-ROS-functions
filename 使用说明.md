# ROS2 功能 Python 封装 - 使用说明

## 目录
1. [快速开始](#快速开始)
2. [环境要求](#环境要求)
3. [模块说明](#模块说明)
4. [基础使用教程](#基础使用教程)
5. [进阶应用示例](#进阶应用示例)
6. [常见问题](#常见问题)
7. [API 参考](#api-参考)

---

## 快速开始

### 最简单的示例

```python
from robot_lib_system import RobotSystem

# 创建机器人对象
robot = RobotSystem()

# 初始化机器人（麦轮车型）
if robot.initialize("mec"):
    print("初始化成功！")
    
    # 获取电池电压
    voltage = robot.get_battery_voltage()
    print(f"电池电压: {voltage}V")
    
    # 安全关闭
    robot.shutdown()
```

### 5 分钟入门

```python
from robot_lib_system import RobotSystem
from robot_lib_motion import RobotMotion
import time

# 1. 初始化系统
system = RobotSystem()
system.initialize("mec")

# 2. 创建运动控制对象
motion = RobotMotion("mec")

# 3. 控制机器人前进
motion.set_velocity(0.2, 0.0, 0.0)  # 0.2m/s 前进
time.sleep(3)
motion.stop()

# 4. 原地旋转 90 度
motion.rotate_angle(90)

# 5. 关闭系统
system.shutdown()
```

---

## 环境要求

### 硬件要求
- Wheeltec 机器人底盘（阿克曼/差速/麦轮）
- USB 相机（可选）
- 激光雷达（可选）
- 机械臂（可选）

### 软件要求
- Ubuntu 22.04
- ROS2 Humble
- Python 3.10+
- Wheeltec 功能包已编译

### 检查环境

```bash
# 1. 检查 ROS2 是否安装
ros2 --version

# 2. 检查功能包是否编译
ros2 pkg list | grep wheeltec

# 3. Source ROS2 环境
source /opt/ros/humble/setup.bash
source ~/wheeltec_ros2/install/setup.bash
```

---

## 模块说明

### 核心模块

| 模块文件 | 功能 | 主要类 |
|---------|------|--------|
| `robot_lib_system.py` | 系统管理 | RobotSystem |
| `robot_lib_motion.py` | 底盘运动控制 | RobotMotion |
| `robot_lib_arm.py` | 机械臂控制 | RobotArm |
| `robot_lib_sensors.py` | 感知功能 | RobotSensors |
| `robot_lib_navigation.py` | 建图导航 | RobotNavigation |

### 示例应用

| 应用文件 | 说明 |
|---------|------|
| `system_app.py` | 系统管理示例 |
| `motion_app.py` | 运动控制示例 |
| `arm_app.py` | 机械臂控制示例 |
| `sensors_app.py` | 感知功能示例 |
| `navigation_app.py` | 建图导航示例 |

---

## 基础使用教程

### 1. 系统管理

#### 初始化和关闭

```python
from robot_lib_system import RobotSystem

robot = RobotSystem()

# 初始化不同车型
robot.initialize("mec")   # 麦轮
# robot.initialize("diff")  # 差速
# robot.initialize("akm")   # 阿克曼

# 使用完毕后关闭
robot.shutdown()
```

#### 电池监控

```python
# 持续监控电池电压
while True:
    voltage = robot.get_battery_voltage()
    
    if voltage < 10.5:
        print("⚠️ 低电量警告！")
        robot.emergency_stop()
        break
    
    print(f"电压: {voltage}V")
    time.sleep(5)
```

### 2. 底盘运动控制

#### 基本速度控制

```python
from robot_lib_motion import RobotMotion

motion = RobotMotion("mec")

# 前进
motion.set_velocity(0.3, 0.0, 0.0)  # vx=0.3m/s
time.sleep(2)

# 左移（仅麦轮）
motion.set_velocity(0.0, 0.2, 0.0)  # vy=0.2m/s
time.sleep(2)

# 原地旋转
motion.set_velocity(0.0, 0.0, 0.5)  # wz=0.5rad/s
time.sleep(2)

# 停止
motion.stop()
```

#### 精确移动

```python
# 麦轮：移动到指定位置
motion.move_distance_mecanum(
    distance_x=1.0,   # 前进 1 米
    distance_y=0.5,   # 左移 0.5 米
    speed_x=0.3,
    speed_y=0.2
)

# 差速/阿克曼：直线移动
motion.move_distance_ackermann_diff(
    distance=2.0,     # 移动 2 米
    speed=0.3         # 速度 0.3m/s
)

# 旋转角度
motion.rotate_angle(90)    # 逆时针 90 度
motion.rotate_angle(-180)  # 顺时针 180 度
```

#### 获取传感器数据

```python
# 获取位姿
pose = motion.get_robot_pose()
print(f"位置: ({pose['x']}, {pose['y']})")
print(f"朝向: {math.degrees(pose['theta'])}°")

# 获取 IMU 数据
imu = motion.get_imu_data()
print(f"角速度 Z: {imu['gyro_z']} rad/s")
print(f"加速度 Z: {imu['accel_z']} m/s²")

# 获取车轮速度
speeds = motion.get_wheel_speeds()
print(f"车轮速度: {speeds}")
```

### 3. 机械臂控制

#### 关节空间控制

```python
from robot_lib_arm import RobotArm

arm = RobotArm()

# 复位到初始姿态
arm.arm_home()

# 设置关节角度
arm.set_joint_angles(
    joint1=45,   # 大臂 45 度
    joint2=-30   # 小臂 -30 度
)

# 设置云台角度
arm.set_yaw_angle(90)

# 控制夹爪
arm.set_gripper(0)   # 闭合
time.sleep(1)
arm.set_gripper(10)  # 打开
```

#### 笛卡尔空间控制

```python
# 移动末端到指定位置（单位：mm）
arm.set_arm_position(x=250, y=100)

# 获取当前末端位置
x, y = arm.get_arm_pose_xy()
print(f"末端位置: ({x}mm, {y}mm)")

# 获取关节状态
j1, j2, yaw = arm.get_joint_states()
print(f"关节角度: {j1}°, {j2}°, {yaw}°")
```

#### 抓取和放置

```python
# 完整的抓取流程
pick_pos = (250, 50)
place_pos = (250, -50)

# 1. 移动到抓取位置
arm.set_arm_position(pick_pos[0], pick_pos[1])
time.sleep(2)

# 2. 闭合夹爪（抓取）
arm.set_gripper(0)
time.sleep(1)

# 3. 提升
arm.set_arm_position(pick_pos[0], pick_pos[1] + 50)
time.sleep(2)

# 4. 移动到放置位置
arm.set_arm_position(place_pos[0], place_pos[1])
time.sleep(2)

# 5. 打开夹爪（放置）
arm.set_gripper(10)
time.sleep(1)

# 6. 复位
arm.arm_home()
```

### 4. 感知功能

#### 启动传感器

```python
from robot_lib_sensors import RobotSensors

sensors = RobotSensors()

# 启动雷达
sensors.launch_lidar(visualize=True)  # 带 RViz 可视化

# 启动相机
sensors.launch_camera(visualize=True)  # 带图像窗口
```

#### 获取传感器数据

```python
# 获取雷达距离
dist_front = sensors.get_lidar_distance(0)      # 正前方
dist_left = sensors.get_lidar_distance(90)      # 左侧
dist_right = sensors.get_lidar_distance(-90)    # 右侧

print(f"前: {dist_front}m, 左: {dist_left}m, 右: {dist_right}m")

# 拍照
sensors.get_camera_frame("photo.jpg")
```

#### 视觉应用

```python
# 视觉跟随（自动跟随红色物体）
sensors.start_visual_follow(
    color='red',
    control_robot=True  # 自动控制底盘
)

# 视觉巡线（沿黑线行驶）
sensors.start_line_tracking(
    color='black',
    control_robot=True
)

# 雷达跟随（保持 0.8 米距离）
sensors.start_lidar_follow(target_dist=0.8)

# 停止应用
sensors.stop_application()
```

#### 不控制底盘的视觉检测

```python
# 只检测目标，不控制机器人
def on_detect(info):
    if info['detected']:
        print(f"检测到目标: 中心({info['center_x']}, {info['center_y']})")
    else:
        print("未检测到目标")

sensors.start_visual_follow(
    color='blue',
    control_robot=False,
    callback=on_detect
)
```

### 5. 建图与导航

#### SLAM 建图

```python
from robot_lib_navigation import RobotNavigation

nav = RobotNavigation()

# 启动建图（gmapping 算法）
nav.start_mapping("gmapping", visualize=True)

# 键盘控制建图
nav.start_keyboard_control()  # 用 WASD 控制，Ctrl+C 退出

# 保存地图
nav.save_map("my_office_map")

# 关闭建图
nav.shutdown()
```

#### 自主导航

```python
# 加载地图并启动导航
nav.load_map_and_start_navigation("my_office_map", visualize=True)

# 导航到目标点
nav.move_to_goal(
    x=2.0,        # X 坐标（米）
    y=1.5,        # Y 坐标（米）
    theta=90      # 朝向角度（度）
)

# 等待到达
while nav.get_navigation_status() == "navigating":
    print("导航中...")
    time.sleep(1)

print("到达目标！")

# 取消导航（如果需要）
nav.cancel_navigation()
```

#### 多点巡航

```python
waypoints = [
    (1.0, 1.0, 0, "点1"),
    (2.0, 1.0, 90, "点2"),
    (2.0, 2.0, 180, "点3"),
    (0.0, 0.0, 0, "原点"),
]

for x, y, theta, name in waypoints:
    print(f"前往 {name}...")
    nav.move_to_goal(x, y, theta)
    time.sleep(15)  # 等待到达
```

---

## 进阶应用示例

### 1. 自动建图

让机器人自动移动并建图，无需手动控制：

```python
from robot_lib_navigation import RobotNavigation
from robot_lib_motion import RobotMotion
from robot_lib_sensors import RobotSensors

# 初始化
sensors = RobotSensors()
sensors.launch_lidar()

nav = RobotNavigation()
nav.start_mapping("gmapping")

motion = RobotMotion("mec")

# 自动探索（走正方形）
for _ in range(2):
    motion.move_distance_mecanum(2.0, 0.0, 0.3, 0.0)  # 前进
    motion.rotate_angle(90)                           # 左转
    time.sleep(1)

# 保存地图
nav.save_map("auto_map")
```

### 2. 智能避障巡航

结合雷达数据的智能导航：

```python
from robot_lib_motion import RobotMotion
from robot_lib_sensors import RobotSensors

motion = RobotMotion("diff")
sensors = RobotSensors()
sensors.launch_lidar()

while True:
    # 检测前方距离
    dist = sensors.get_lidar_distance(0)
    
    if dist > 1.0:
        # 前方安全，前进
        motion.set_velocity(0.3, 0.0, 0.0)
    elif dist > 0.5:
        # 接近障碍物，减速
        motion.set_velocity(0.1, 0.0, 0.0)
    else:
        # 太近，停止并转向
        motion.stop()
        motion.rotate_angle(90)
    
    time.sleep(0.5)
```

### 3. 视觉导航

使用视觉跟随进行导航：

```python
from robot_lib_sensors import RobotSensors
from robot_lib_motion import RobotMotion

sensors = RobotSensors()
motion = RobotMotion("mec")

# 启动相机和雷达
sensors.launch_camera()
sensors.launch_lidar()

# 跟随红色目标，但手动控制距离
def on_target(info):
    if info['detected']:
        # 根据目标面积控制距离
        if info['area'] < 5000:
            motion.set_velocity(0.2, 0.0, 0.0)  # 靠近
        elif info['area'] > 8000:
            motion.set_velocity(-0.2, 0.0, 0.0)  # 后退
        else:
            motion.stop()  # 距离合适

sensors.start_visual_follow(
    'red',
    control_robot=False,
    callback=on_target
)
```

### 4. 多机器人协同

控制多个机器人协同工作：

```python
# 机器人 1：建图
robot1_nav = RobotNavigation()
robot1_nav.start_mapping("gmapping")
# ... 控制机器人 1 建图 ...

# 机器人 2：跟随
robot2_sensors = RobotSensors()
robot2_sensors.start_lidar_follow(0.5)

# 机器人 3：巡线
robot3_sensors = RobotSensors()
robot3_sensors.start_line_tracking('black')
```

---

## 常见问题

### Q1: 初始化失败怎么办？

**A**: 检查以下几点：
1. ROS2 环境是否正确 source
2. 功能包是否编译成功
3. 硬件（串口/雷达）是否正确连接
4. 查看错误日志：
   ```python
   robot = RobotSystem()
   robot.initialize("mec")
   # 如果失败，查看终端输出的错误信息
   ```

### Q2: 获取不到传感器数据？

**A**: 确认：
1. 传感器驱动是否启动（`sensors.launch_lidar()` 或 `sensors.launch_camera()`）
2. 话题是否正常发布：
   ```bash
   ros2 topic list  # 查看所有话题
   ros2 topic hz /scan  # 查看雷达话题频率
   ```
3. 等待足够的初始化时间（2-3秒）

### Q3: 运动控制不准确？

**A**: 可能原因：
1. 地面太滑或不平整
2. 里程计漂移（长距离运动累积误差）
3. 速度设置过大
4. 解决方法：
   - 降低运动速度
   - 使用闭环控制（`move_distance`, `rotate_angle`）
   - 校准里程计参数

### Q4: 导航失败或机器人不动？

**A**: 检查：
1. 地图是否正确加载
2. 机器人在地图中的初始位置是否正确
3. 目标点是否在自由空间内（不在障碍物上）
4. 导航堆栈是否完全初始化（需要 15 秒）
5. 使用 RViz 查看路径规划情况

### Q5: 如何处理异常和错误？

**A**: 使用 try-except-finally 结构：
```python
from robot_lib_system import RobotSystem

robot = RobotSystem()

try:
    robot.initialize("mec")
    # ... 你的代码 ...
    
except KeyboardInterrupt:
    print("用户中断")
    
except Exception as e:
    print(f"发生错误: {e}")
    
finally:
    # 无论如何都会执行
    robot.shutdown()
```

### Q6: 程序退出后机器人还在运动？

**A**: 确保调用 `shutdown()` 或 `stop()`：
```python
# 方法 1：手动调用
robot.shutdown()

# 方法 2：使用 with 语句（如果类支持）
# 方法 3：在 finally 中调用
try:
    # 代码
    pass
finally:
    robot.shutdown()
```

---

## API 参考

### RobotSystem 类

| 方法 | 参数 | 返回值 | 说明 |
|-----|------|--------|------|
| `initialize(robot_type)` | robot_type: str ("akm"/"diff"/"mec") | bool | 初始化机器人系统 |
| `shutdown()` | 无 | 无 | 关闭机器人系统 |
| `get_battery_voltage()` | 无 | float | 获取电池电压（V） |
| `emergency_stop()` | 无 | 无 | 紧急停止 |
| `get_software_version()` | 无 | str | 获取软件版本 |

### RobotMotion 类

| 方法 | 参数 | 返回值 | 说明 |
|-----|------|--------|------|
| `set_velocity(vx, vy, wz)` | vx, vy, wz: float | 无 | 设置速度 (m/s, rad/s) |
| `stop()` | 无 | 无 | 停止运动 |
| `move_distance_mecanum(dx, dy, sx, sy)` | 距离和速度 | bool | 麦轮移动指定距离 |
| `move_distance_ackermann_diff(d, s, r)` | 距离、速度、半径 | bool | 差速/阿克曼移动 |
| `rotate_angle(angle, speed)` | angle: float, speed: float | bool | 旋转指定角度 |
| `get_robot_pose()` | 无 | dict | 获取位姿 |
| `get_imu_data()` | 无 | dict | 获取 IMU 数据 |
| `get_wheel_speeds()` | 无 | list | 获取车轮速度 |

### RobotArm 类

| 方法 | 参数 | 返回值 | 说明 |
|-----|------|--------|------|
| `arm_home()` | 无 | bool | 机械臂复位 |
| `set_joint_angles(j1, j2)` | j1, j2: float (度) | bool | 设置关节角度 |
| `set_arm_position(x, y)` | x, y: float (mm) | bool | 设置末端位置 |
| `set_gripper(value)` | value: 0-10 | bool | 控制夹爪 |
| `get_arm_pose_xy()` | 无 | tuple | 获取末端坐标 |
| `get_joint_states()` | 无 | tuple | 获取关节状态 |

### RobotSensors 类

| 方法 | 参数 | 返回值 | 说明 |
|-----|------|--------|------|
| `launch_lidar(visualize)` | visualize: bool | bool | 启动雷达 |
| `stop_lidar()` | 无 | 无 | 停止雷达 |
| `launch_camera(visualize)` | visualize: bool | bool | 启动相机 |
| `stop_camera()` | 无 | 无 | 停止相机 |
| `start_visual_follow(color, ...)` | color: str, ... | bool | 视觉跟随 |
| `start_line_tracking(color, ...)` | color: str, ... | bool | 视觉巡线 |
| `start_lidar_follow(dist)` | dist: float | bool | 雷达跟随 |
| `stop_application()` | 无 | 无 | 停止应用 |
| `get_lidar_distance(angle)` | angle: float (度) | float | 获取雷达距离 |

### RobotNavigation 类

| 方法 | 参数 | 返回值 | 说明 |
|-----|------|--------|------|
| `start_mapping(method, ...)` | method: str, ... | bool | 启动建图 |
| `save_map(name)` | name: str | bool | 保存地图 |
| `load_map_and_start_navigation(...)` | name: str, ... | bool | 加载地图并启动导航 |
| `move_to_goal(x, y, theta)` | x, y, theta: float | bool | 导航到目标 |
| `cancel_navigation()` | 无 | bool | 取消导航 |
| `get_navigation_status()` | 无 | str | 获取导航状态 |

---

## 获取帮助

### 运行示例应用

每个模块都有对应的示例应用，可以直接运行：

```bash
# 系统管理示例
python3 system_app.py

# 运动控制示例
python3 motion_app.py

# 机械臂控制示例
python3 arm_app.py

# 感知功能示例
python3 sensors_app.py

# 建图导航示例
python3 navigation_app.py
```

### 查看源代码

所有函数都有详细的中文注释，可以直接查看源代码了解实现细节。

### 联系方式

如有问题，请通过以下方式联系：
- GitHub Issues
- 邮件
- 技术支持

---

**文档版本**：v1.0  
**最后更新**：2026-02-02  
**祝使用愉快！** 🚀
